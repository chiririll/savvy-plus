name: savvy-go
arch: amd64
platform: linux
version: ${APP_VERSION}
version_schema: semver
release: "1"
section: web
priority: optional
maintainer: Kirill Chizhov <sstive39@gmail.com>
homepage: https://github.com/chiririll/savvy-plus
license: MIT
description: |-
  Selfhosted expense tracker with multi-currency support.
  .
  Installs the Savvy Go binary, static SPA assets, and a systemd unit.
  SQLite data lives in /var/lib/savvy-go.

depends:
  - adduser

contents:
  - src: ../../dist/savvy-go
    dst: /usr/bin/savvy-go
    file_info:
      mode: 0755
  - src: ../../dist/public
    dst: /opt/savvy-go/public
    type: tree
  - src: savvy-go.service
    dst: /usr/lib/systemd/system/savvy-go.service
  - src: install.env
    dst: /etc/savvy-go/install.env
    type: config
  - dst: /var/lib/savvy-go
    type: dir
    file_info:
      mode: 0775
      owner: www-data
      group: www-data

scripts:
  postinstall: postinst
  preremove: prerm
  postremove: postrm
