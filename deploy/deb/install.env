# Optional settings for Savvy. systemd units and the package postinst source this file.
# After changing values, re-run:
#   sudo APP_DIR=/opt/savvy DATA_DIR=/var/lib/savvy /opt/savvy/scripts/bootstrap.sh
#   sudo systemctl restart savvy-queue savvy-scheduler
#
# APP_URL=http://localhost
# TZ=UTC
# SEED_DEMO=false
